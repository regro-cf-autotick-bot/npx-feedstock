{% set name = "npx" %}
{% set version = "0.0.23" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: be8946891b581a37f7f6e06372b8d0518cb5fc541e20f70d698c11b6ce116705

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3
    - pip
    - setuptools
  run:
    - python >=3
    - importlib_metadata
    - numpy

test:
  imports:
    - npx
  requires:
    - exdown
    - pytest
  source_files:
    - README.md
    - tests
  commands:
    - python -m pytest

about:
  home: https://github.com/nschloe/npx
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Useful fixes and extensions for NumPy
  dev_url: https://github.com/nschloe/npx

extra:
  recipe-maintainers:
    - nschloe
